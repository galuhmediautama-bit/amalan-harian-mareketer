import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.amalanmarketer.berkah',
  appName: 'Amalan Marketer Berkah',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;

